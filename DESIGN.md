# ReplayTutor Design System

> 绘图工作台实施注记（2026-08-02）：正式工具目录固定为 40 项，并由统一工具注册表
> 校验唯一 ID、锚点数量和 renderer。线类与专业分析工具使用分组展开；对象选中后提供
> 样式、复制、锁定、隐藏与删除操作，所有持久修改进入追加式修订事件。

> Internationalization is part of layout acceptance: English and Simplified Chinese must both fit without clipping. UI labels are translation keys; stable IDs and trading semantics are never localized.

状态：Approved v1.0  
更新时间：2026-07-19  
适用范围：桌面 Web 应用、后续桌面壳及产品内所有图表、交易、回放和 Tutor 界面

## 1. 设计结论

ReplayTutor 采用 **TradingView 式专业图表工作台范式**：图表占据主舞台，工具围绕画布停靠，信息密度高，状态反馈即时，鼠标与键盘均可高效操作。

参考的是成熟的信息架构和交互范式，不复制 TradingView 的品牌、Logo、专有图标、文案、像素级布局或闭源 Charting Library。所有颜色、组件、图标和交互实现均为 ReplayTutor 自有系统。

用户第一次看到产品时应记住：

> 这是一个会记住“当时为什么交易”的专业图表工作台。

视觉关键词：**工业化、克制、高密度、图表主导、证据可追溯**。

## 2. 产品设计原则

1. **图表优先**：默认可视面积优先给行情，不用大卡片和装饰抢占画布。
2. **时间视角始终可见**：实时、回放中、事后复盘使用不同且持续可见的状态表达。
3. **颜色表达语义，不表达装饰**：蓝色用于操作和回放，红绿用于交易语义，黄色用于风险。
4. **AI 结论必须可回跳**：观察、推断、规则检查均能定位到 K 线、订单、成交或笔记。
5. **高频操作不阻塞图表**：订单编辑、回放控制和 Tutor 对话采用停靠面板或非模态浮层。
6. **同一状态只有一个真源**：图表、订单面板、回放条和 Tutor 共用同一个 `frame_id` 与 `visible_at`。
7. **专业不等于拥挤**：用对齐、层级和渐进披露控制复杂度，不靠放大字号和留白稀释信息。

## 3. 应用外壳

桌面端是主产品。目标宽度为 1440–1920px，最低完整工作宽度为 1180px。

```text
┌──────────────────────────────────────────────────────────────────────────────┐
│ App/品种  周期  图表  指标  布局  撤销       数据状态   REPLAY  保存   设置 │ 40
├────┬──────────────────────────────────────────────┬───────────────┬─────────┤
│绘图│                                              │ Tutor / 自选  │ 右工具坞│
│工具│                   主图表                     │ 证据 / 规则   │ 图标栏  │
│ 44 │                                              │ 默认 360px    │   40    │
│    ├──────────────────────────────────────────────┴───────────────┴─────────┤
│    │  回放日期  ◀  ▶  播放  速度  随机片段  visible_at  结束回放          │ 44
├────┴────────────────────────────────────────────────────────────────────────┤
│ 订单 | 持仓 | 成交 | P&L | 笔记 | 事件                         折叠/展开 │ 28/240
└──────────────────────────────────────────────────────────────────────────────┘
```

### 3.1 尺寸规则

- 顶部工具栏：40px，固定。
- 左侧绘图工具栏：44px，固定；不显示常驻文字标签，悬停后出现说明。工具族通过紧邻
  图标的分组面板展开，面板不得挤压或遮挡主工作台布局。
- 右侧工具图标栏：40px，固定。
- 右侧停靠面板：默认 360px，可拖动至 300–520px，可折叠。
- 回放控制条：44px，仅在准备回放、回放中和暂停时显示。
- 底部面板：折叠时 28px，展开默认 240px，可拖动至视口高度的 55%。
- 图表是剩余空间的唯一弹性区域，最小宽度 560px、最小高度 360px。
- 分隔条命中区 6px，视觉线宽 1px。

### 3.2 响应式边界

- `>= 1440px`：完整工具栏、360px Tutor、底部面板。
- `1180–1439px`：右侧面板默认 320px，次要工具收进 More 菜单。
- `900–1179px`：右侧面板改为覆盖式抽屉；底部面板一次只显示一个页签。
- `< 900px`：MVP 只提供查看、回放控制、笔记和 Tutor；不承诺完整画线与复杂下单体验。

## 4. 导航与信息架构

顶部左上角的产品菜单切换一级空间：

1. **工作台**：图表、回放、虚拟交易、Tutor。
2. **交易日志**：训练交易与真实成交记录。
3. **复盘中心**：单笔、会话、每日和周期报告。
4. **Playbook**：策略、检查表、风险规则。
5. **数据中心**：行情覆盖、快照、导入和质量。
6. **设置**：Agent、市场偏好、费用和隐私。

工作台不使用常驻的大型主导航侧栏。用户进入训练后，画布比导航更重要；一级空间通过产品菜单和 `Cmd/Ctrl + K` 命令面板访问。

## 5. 工作台区域

### 5.1 顶部工具栏

从左到右固定为：

- 产品菜单与工作区名称。
- 品种搜索。
- 周期。
- 图表类型。
- 指标。
- 模板与布局。
- 撤销/重做。
- 弹性空白。
- 数据质量状态。
- 模式胶囊：`LIVE`、`REPLAY`、`REVIEW`。
- 保存状态、截图、设置。

品种、周期和回放模式是强上下文，始终直接显示；低频功能不得挤占首屏。
工作台周期条固定提供 `1m / 5m / 15m / 1h / 4h / 1d`。切换图表周期只改变当前
`visible_at` 以内的聚合视图，不改变会话游标、撮合基础周期或账本。

### 5.2 左侧绘图工具栏

首期提供：光标、十字光标、趋势线、平行通道、斐波那契回撤、水平线、矩形、文字、
测量、多空仓位计划、磁吸、锁定、隐藏、删除、撤销和重做。

- 同组工具长按或点击角标展开子菜单。
- 当前工具使用 `action.primary` 背景和高对比图标。
- `Esc` 返回光标；连续绘制必须显式开启。
- 锁定、隐藏和删除是作用于图层的操作，不在每个工具中重复。
- 用户对象解锁时可直接拖动控制点；任何拖动修订都追加审计事件，并同步更新有效
  metadata。撤销和重做只操作当前会话内的图表对象历史，不修改订单、成交或账本。

### 5.2.1 键盘操作

- `Cmd/Ctrl + K` 打开 ReplayTutor 自有命令面板；`?` 打开快捷键说明。
- 画线快捷键为 `Alt/Option + T/H/V/C/F`，矩形区域为 `Alt/Option + Shift + R`；工具悬停提示同步显示快捷键。
- 直接输入 `1 / 5 / 15 / 60 / 240` 切换图表周期；周期切换仍只聚合 `visible_at` 以内的数据。
- `Space` 播放/暂停，`→` 前进一根，`Cmd/Ctrl + →` 前进十根；不提供可能越过事件流或窥视未来的任意回退。
- `Cmd/Ctrl + S` 只把周期、图层可见性、锁定、磁吸和连续绘制偏好保存到本地，不写入回放、订单或账本事实。
- `Shift + B/S` 与 `Shift + Alt/Option + B/S` 只预填模拟订单草稿，永远不绕过计划门禁和显式提交确认。
- 当前不可用的品种直输、指定日期跳转和指标入口必须在命令面板中明确标记，不得以无响应按钮伪装完成。

### 5.3 右侧工具坞

右侧图标栏顺序固定：

1. Tutor
2. 自选
3. 证据
4. Playbook
5. 品种详情
6. 数据质量

点击当前图标折叠面板，点击其他图标原位切换。Tutor 是回放会话中的默认面板，自选是普通浏览状态的默认面板。

Tutor 内部页签：`对话`、`证据`、`检查表`、`报告`。回答结构固定为结论、观察、推断、风险、下一问；观察和规则判断旁必须有证据跳转。

### 5.4 底部面板

页签固定为：`订单`、`持仓`、`成交`、`P&L`、`笔记`、`事件`。

- 新订单、拒单、成交和风险警告以页签圆点提示，不自动抢占焦点。
- 表格采用固定表头、右对齐数字、等宽数字和列宽记忆。
- 双击成交或事件，图表跳转到对应 K 线并短暂高亮。
- 面板折叠后仍显示当前持仓数量、未实现 P&L 和工作订单数。

## 6. 回放体验

### 6.1 模式表达

`LIVE`、`REPLAY`、`REVIEW` 不只依赖文案：

- `LIVE`：中性深色工具栏，不出现回放边界。
- `REPLAY`：顶部模式胶囊、回放条和图表最右可见边界统一使用蓝色；边界标注 `Visible to HH:mm`。
- `REVIEW`：使用琥珀色模式胶囊，明确允许查看完整结果。

任何模式切换都需要显式动作。结束回放后先显示会话完成页，不自动跳回实时行情。

### 6.2 启动流程

```text
选择市场/品种 → 选择 Snapshot 版本 → 选择从头、指定时间或随机片段
→ 确认初始资金/费用/撮合规则 → 数据质量检查 → 开始回放
```

随机训练开始后隐藏真实日期，只显示会话相对时间；完成会话后才揭示品种日期和完整行情。

### 6.3 回放控制

- 播放/暂停是主按钮。
- 逐根前进紧邻主按钮。
- 速度为 `1× / 2× / 5× / 10× / 20×`。
- 跳转只能前往当前可见历史或明确重新开始，不能通过日期选择器窥视未来。
- 快捷键：`Space` 播放/暂停，`.` 下一根，`,` 上一可见检查点，`Shift + R` 开始/结束回放。
- 回放进行时，品种、撮合基础周期或快照变化触发确认并创建新会话，不静默改变现有结果。
- 图表显示周期可在 `1m / 5m / 15m / 1h / 4h / 1d` 间即时切换，不创建新会话。

### 6.4 时间安全交互

- UI 只能渲染服务端签发的 `ReplayFrame`。
- 用户看见完整 K 线后提交的订单，最早从下一根 K 线参与撮合。
- 当前 K 线的高低价不得反向用于成交刚刚提交的订单。
- 回放中的自选报价、品种详情、P&L、Tutor 和数据窗口均受同一 `visible_at` 约束。
- 未来区域为空白画布，不显示淡化 K 线、事件图标、轴范围暗示或缩略图。

## 7. 订单与图表交易

订单面板支持停靠和临时浮层两种形态，默认停靠在右侧工具坞。

### 7.1 订单票据

字段顺序：方向、类型、价格、数量、名义金额、止损、止盈、有效期、费用、风险金额、账户占比。

- 买入和卖出必须同时有文字与图标，不能只靠红绿。
- 风险金额比名义金额更突出。
- tick size、lot size、T+1、涨跌停等由规则适配器返回并在字段旁解释。
- 默认启用提交确认；高频无确认模式属于后续用户设置。

### 7.2 图表投单

- 右键价格或价格轴 `+` 可创建投单草稿。
- 草稿线可拖动，订单票据同步更新。
- 同一时刻只允许一个未提交投单草稿。
- 修改必须显示“确认”和“放弃”，不因失焦自动提交。
- 工作订单、持仓均可在图表上选择和管理，但任何变更都经过确定性验证。

### 7.3 标记系统

- 买入：向上箭头 + `B`。
- 卖出：向下箭头 + `S`。
- 止损：盾牌断开图形 + `SL`。
- 止盈：目标图形 + `TP`。
- 拒单：描边警告菱形。
- AI 证据：小型引用标记，不与成交标记共用形状。

标记拥挤时按时间聚合，点击后展开，不允许覆盖 K 线主体信息。

## 8. AI Tutor

Tutor 是工作台工具，不是独立聊天产品。

- 输入框固定在面板底部，上方显示当前上下文：品种、周期、`visible_at`、选择的成交或规则。
- 默认问题使用操作型快捷入口，如“评价这次入场”“检查是否符合计划”“解释拒单原因”。
- 结论最多两行，详细证据渐进展开。
- `观察` 只能描述确定性事实；`推断` 显示置信度；`未知` 不隐藏。
- 点击证据在图表、底部表格和 Tutor 中形成三向联动。
- 所有已注册绘图工具都可以进入 Tutor 上下文。用户在“图层对象”中显式加入
  趋势线、区域、计划开仓、加减仓、止损、止盈或风险收益框；输入框上方的
  Context Tray 必须在发送前展示完整对象清单。
- 每次提问由服务端把选中对象固化为不可变 `ChartContextBundle`。旧回答绑定原始
  对象坐标，不因用户之后移动、修改或删除图形而漂移。
- Agent 运行状态使用细进度条和阶段文本，不使用拟人化打字气泡。
- Agent 失败只影响回答区域，不遮挡或冻结图表与回放。

## 9. 视觉系统

### 9.1 字体

- UI/正文：`IBM Plex Sans`，中文回退 `Noto Sans SC`, `PingFang SC`, sans-serif。
- 数据/价格/时间：`IBM Plex Mono`，启用 `font-variant-numeric: tabular-nums`。
- 图表轴可使用 UI 字体的等宽数字特性；长 ID 和代码使用 Mono。
- 字体首选随应用自托管，避免离线模式依赖第三方 CDN。

字号：

| Token | 大小/行高 | 用途 |
|---|---|---|
| `text.xs` | 11/16 | 轴标签、徽标、辅助状态 |
| `text.sm` | 12/18 | 工具栏、紧凑表格 |
| `text.base` | 13/20 | 默认 UI 与正文 |
| `text.md` | 14/22 | Tutor 正文、表单说明 |
| `text.lg` | 16/24 | 面板标题 |
| `text.xl` | 20/28 | 报告标题 |
| `text.2xl` | 28/36 | 空状态与会话总结 |

### 9.2 暗色主题

暗色是默认和首要设计目标。

| Token | Hex | 用途 |
|---|---|---|
| `surface.canvas` | `#0B0E13` | 图表外背景 |
| `surface.chart` | `#0D1117` | 图表画布 |
| `surface.panel` | `#11161E` | 停靠面板 |
| `surface.raised` | `#181F2A` | 菜单、选中、浮层 |
| `surface.overlay` | `#202937` | 模态层和高层浮层 |
| `border.subtle` | `#232C38` | 分隔线与网格 |
| `border.strong` | `#344154` | 输入和焦点前边界 |
| `text.primary` | `#E6EDF5` | 主文本 |
| `text.secondary` | `#99A6B6` | 辅助文本 |
| `text.muted` | `#687789` | 禁用与弱信息 |
| `action.primary` | `#3B82F6` | 主操作、回放边界 |
| `action.hover` | `#5794F8` | 主操作悬停 |
| `trade.long` | `#21C58B` | 买入/做多 |
| `trade.short` | `#F05B72` | 卖出/做空 |
| `warning` | `#E9A23B` | 风险、Review 模式 |
| `danger` | `#EE5968` | 错误与破坏性操作 |
| `info` | `#55A7FF` | 信息和数据状态 |

图表网格线使用 `border.subtle` 的 45% 透明度。K 线颜色可随地区偏好切换，但买卖动作颜色和标记形状保持不变。

### 9.3 亮色主题

| Token | Hex |
|---|---|
| `surface.canvas` | `#F3F6FA` |
| `surface.chart` | `#FFFFFF` |
| `surface.panel` | `#F8FAFC` |
| `surface.raised` | `#EAF0F6` |
| `border.subtle` | `#D7E0EA` |
| `border.strong` | `#AEBBC9` |
| `text.primary` | `#17202B` |
| `text.secondary` | `#5E6B7A` |
| `text.muted` | `#8491A0` |
| `action.primary` | `#2563EB` |
| `trade.long` | `#068A63` |
| `trade.short` | `#D83F59` |
| `warning` | `#B7791F` |

亮色不是把暗色简单反转；阴影、网格、交易色和焦点环分别校准。

### 9.4 间距、圆角与阴影

- 基础单位：4px。
- 间距：2、4、8、12、16、20、24、32、48。
- 控件高度：紧凑 28px，默认 32px，强调 36px。
- 表格行高：32px；报告型表格 36px。
- 圆角：按钮/输入 4px，菜单/面板 6px，模态层 8px，胶囊 999px。
- 不使用所有组件统一的大圆角。
- 停靠面板不使用阴影，仅用边界；浮层使用 `0 12px 32px rgba(0,0,0,.32)`。

### 9.5 图标

- 使用统一 16/18/20px 线性图标集，默认线宽 1.5px。
- 可采用 Lucide 作为基础并为订单、回放、证据设计自有图标。
- 不复制 TradingView 图标轮廓。
- 无文字工具必须有 tooltip、快捷键和可访问名称。

## 10. 交互状态

每个异步区域独立处理：

- `idle`：提供下一步，不显示空白骨架。
- `loading`：超过 200ms 才显示骨架；图表加载保留轴和布局，避免跳动。
- `stale`：旧数据仍可见，顶部显示“数据可能过期”。
- `offline`：本地回放继续可用；需要模型服务的 Tutor 明确不可用。
- `empty`：解释缺少什么数据以及如何导入。
- `error`：错误就地出现，包含原因、影响范围和恢复动作。
- `permission_denied`：不自动放宽权限。

行情下载属于跨页面任务：运行时在 AppShell 顶部持续显示品种与进度，点击可回到数据
中心；页面切换或刷新不能取消任务。完成后刷新 Snapshot 索引，但不抢占当前页面焦点。

Toast 只用于已完成且无需立即处理的反馈。拒单、数据缺口和未来数据拦截必须在触发位置持续显示。

## 11. 动效

采用最小功能动效：

- 微反馈：80ms。
- 面板、菜单：140ms。
- 抽屉、模式切换：200ms。
- 缓动：进入 `cubic-bezier(.2,.8,.2,1)`，退出 `cubic-bezier(.4,0,1,1)`。
- 回放游标移动跟随数据节奏，不增加装饰性弹性动画。
- `prefers-reduced-motion` 下取消位移，只保留必要透明度变化。

## 12. 键盘与命令

- `Cmd/Ctrl + K`：全局命令面板。
- 输入品种代码：非输入焦点时直接打开品种搜索。
- `Space`：播放/暂停。
- `.`：下一根。
- `Shift + B / Shift + S`：打开买入/卖出票据，不直接提交。
- `Cmd/Ctrl + Enter`：确认当前投单。
- `Esc`：取消草稿或退出当前工具。
- `Alt + 1…6`：切换右侧工具坞。
- `Cmd/Ctrl + J`：展开/折叠底部面板。

所有快捷键都必须可发现、可修改，并避免在文本输入中误触。

## 13. 可访问性

- 文本和关键控件达到 WCAG AA。
- 焦点环为 2px `action.primary`，与背景保留 2px 间隔。
- 所有红绿状态同时使用形状、文字或图案。
- 图表提供当前 K 线的数据表视图和键盘十字光标。
- 拖拽订单必须有键盘等价操作。
- 屏幕阅读器播报订单接受、拒绝、成交和回放模式变化，但不播报每次价格跳动。

## 14. 禁止项

- 不复制 TradingView Logo、品牌名、专有图标或像素级界面。
- 不使用紫色渐变、玻璃拟态、大面积发光、装饰性行情背景。
- 不用卡片网格承载本可用工具栏或表格表达的信息。
- 不在回放中显示实时自选报价、未来事件、最终 P&L 或未来轴范围暗示。
- 不让 AI 文本看起来像确定性系统消息。
- 不用动画掩盖慢查询或 Agent 等待。
- 不在移动端强塞桌面三栏布局。

## 15. 设计验收门禁

每个工作台相关 PR 必须回答：

1. 图表是否仍是主视觉和最大可用区域？
2. 当前模式和 `visible_at` 是否无需打开菜单即可识别？
3. 红绿信息是否有非颜色替代表达？
4. 新功能是否进入正确的工具栏、工具坞或底部面板，而不是新增漂浮卡片？
5. AI 结论是否能定位证据，且没有伪装成系统事实？
6. 1180px 宽度是否仍能完成核心回放和下单流程？
7. 键盘焦点、错误、加载、空状态是否完整？
8. 是否使用自有资产并遵守 TradingView 非复制边界？

## 16. 参考与取舍

参考：

- TradingView Supercharts 的顶部工具栏、左侧绘图工具、右侧工具区和底部面板范式。
- TradingView Bar Replay 的媒体式控制、选取起点和逐根前进心智模型。
- 专业交易终端的紧凑表格、停靠面板与快捷键优先设计。

刻意不同：

- ReplayTutor 回放时所有区域严格共享历史时间，不允许实时报价混入。
- 右侧首要工具是证据型 Tutor，而不是新闻、社区或经纪商交易入口。
- 回放边界是持续可见的核心视觉语义，而非临时工具状态。
- AI 的观察、推断和系统事实在视觉与数据契约上严格分层。

## 17. 决策记录

| 日期 | 决策 | 原因 |
|---|---|---|
| 2026-07-19 | 采用 TradingView 式专业工作台范式 | 用户已有成熟心智模型，图表工具效率优先 |
| 2026-07-19 | 暗色优先、工业化、高密度 | 适合长时间盯盘与密集数据操作 |
| 2026-07-19 | 右侧 Tutor 工具坞 + 底部交易面板 | 保持图表中心地位并支持证据联动 |
| 2026-07-19 | 蓝色作为回放边界与主操作色 | 避免与红绿交易语义冲突 |
| 2026-07-19 | IBM Plex Sans + IBM Plex Mono | 清晰、专业，数据场景下数字辨识度高 |
| 2026-07-19 | 桌面优先，移动端降级 | 复杂画线与模拟交易需要足够画布和精确输入 |
